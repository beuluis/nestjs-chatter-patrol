name: 'Handling feature dist-tag removal'

on:
  pull_request:
    branches:
      - main
    types:
      - closed # pr is merged/closed

jobs:
  remove-feature-tag:
    if: ${{ github.event.action == 'closed' }}
    uses: beuluis/reusable-workflows/.github/workflows/npm-remove-dist-tag.yml@main
    with:
      dist_tag: ${{ github.head_ref }}
    secrets: inherit
